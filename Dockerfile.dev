# Development Dockerfile (optional - for containerizing the app itself)
FROM oven/bun:1

WORKDIR /app

# Copy package files
COPY package.json bun.lockb ./

# Install dependencies
RUN bun install

# Copy source code
COPY . .

# Generate Prisma client
RUN bun run db:generate

EXPOSE 3000 3001

# Start development servers
CMD ["bun", "run", "dev"]